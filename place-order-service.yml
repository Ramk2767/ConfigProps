
server:
  port: 8883

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/place_order_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
